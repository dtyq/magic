name: Build and push super-magic image

on:
  workflow_dispatch:
  push:
    paths:
      - 'backend/super-magic/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build and push super-magic image
    uses: ./.github/workflows/image-build.yml
    with:
      imageName: super-magic
