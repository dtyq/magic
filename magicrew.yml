images:
  magic-service:
    context: ./backend/magic-service
  magic-gateway:
    context: ./backend/magic-gateway
  super-magic:
    context: ./backend/super-magic
  sandbox-gateway:
    context: ./backend/sandbox-gateway
  magic-web:
    context: ./frontend/magic-web
