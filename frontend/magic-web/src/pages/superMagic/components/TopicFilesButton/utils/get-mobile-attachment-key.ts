import { getAttachmentKey } from "./getAttachmentKey"

/**
 * Stable row key for mobile file tree selection; must match collectSelectedItemIds lookups.
 */
export const getMobileAttachmentKey = getAttachmentKey
