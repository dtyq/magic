name: Build and push magic-gateway image

on:
  workflow_dispatch:
  push:
    paths:
      - 'backend/magic-gateway/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build and push magic-gateway image
    uses: ./.github/workflows/image-build.yml
    with:
      imageName: magic-gateway
