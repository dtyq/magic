import { memo } from "react"

/** Open-source stub; enterprise build overlays this file. */
function AddProviderNoticeComponent() {
	return null
}

export const AddProviderNotice = memo(AddProviderNoticeComponent)
